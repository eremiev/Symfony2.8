imports:
    - { resource: config.yml }

framework:
    router:
        resource: '%kernel.root_dir%/config/routing_dev.yml'
        strict_requirements: true
    profiler: { only_exceptions: false }

web_profiler:
    toolbar: true
    intercept_redirects: false

monolog:
    handlers:
        main:
            type: stream
            path: '%kernel.logs_dir%/%kernel.environment%.log'
            level: debug
            channels: ['!event']
        console:
            type: console
            channels: ['!event', '!doctrine']

# Doctrine Configuration
doctrine_mongodb:
    default_database: locations
    default_connection: default
    default_document_manager: default
    connections:
        default:
            server: mongodb://admin:1$g6Mt256@localhost:27017/Symfony
    document_managers:
        default:
            database: Symfony
            mappings:
#                ArticlesBundle: ~
#                PartnersBundle: ~
#                ProductsBundle: ~
#                PlatformsBundle: ~
#                ResourcesBundle: ~
